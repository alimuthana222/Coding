# حلول أسئلة C# - C# Questions Solutions
## دليل المبتدئين - Beginner's Guide

---

## Q1: Define Object Oriented Programming (تعريف البرمجة كائنية التوجه)

**الإجابة / Answer:**

البرمجة كائنية التوجه (OOP) هي أسلوب برمجي يعتمد على مفهوم "الكائنات" (Objects) التي تحتوي على بيانات وسلوكيات معاً.

Object-Oriented Programming (OOP) is a programming paradigm based on the concept of "objects" which contain data and behavior together.

**المبادئ الأساسية الأربعة / Four Main Principles:**

1. **التغليف (Encapsulation)**: إخفاء تفاصيل البيانات وإظهار الوظائف الضرورية فقط
   - Hiding data details and showing only necessary functions

2. **الوراثة (Inheritance)**: قدرة كلاس (فئة) على وراثة خصائص وسلوكيات من كلاس آخر
   - Ability of a class to inherit properties and behaviors from another class

3. **تعدد الأشكال (Polymorphism)**: قدرة الكائنات على اتخاذ أشكال متعددة
   - Ability of objects to take multiple forms

4. **التجريد (Abstraction)**: إخفاء التفاصيل المعقدة وإظهار الوظائف الأساسية
   - Hiding complex details and showing essential features

**مثال بسيط / Simple Example:**
```csharp
// كلاس يمثل سيارة
public class Car
{
    // البيانات (Properties)
    public string Color;
    public string Model;
    
    // السلوك (Methods)
    public void Start()
    {
        Console.WriteLine("السيارة تعمل - Car is starting");
    }
}
```

---

## Q2: Write a complete C# program (كتابة برنامج C# كامل)
**المطلوب: برنامج يحتوي على مصفوفة 10 أعداد صحيحة، يدخلها المستخدم، ثم يعد الأعداد الزوجية والفردية**

**Required: Program with array of 10 integers, user input, count odd and even numbers**

### الحل الكامل / Complete Solution:

```csharp
using System;

class Program
{
    static void Main()
    {
        // إنشاء مصفوفة لتخزين 10 أرقام
        // Create an array to store 10 numbers
        int[] numbers = new int[10];
        
        // متغيرات للعد
        // Variables for counting
        int evenCount = 0;  // عدد الأرقام الزوجية
        int oddCount = 0;   // عدد الأرقام الفردية
        
        // طلب إدخال الأرقام من المستخدم
        // Ask user to input numbers
        Console.WriteLine("الرجاء إدخال 10 أرقام صحيحة:");
        Console.WriteLine("Please enter 10 integer numbers:");
        
        for (int i = 0; i < 10; i++)
        {
            Console.Write($"أدخل الرقم {i + 1}: ");
            numbers[i] = int.Parse(Console.ReadLine());
        }
        
        // استخدام foreach للمرور على كل عنصر في المصفوفة
        // Using foreach to loop through each element
        foreach (int num in numbers)
        {
            // التحقق إذا كان الرقم زوجي أو فردي
            // Check if number is even or odd
            if (num % 2 == 0)
            {
                evenCount++;  // زوجي
            }
            else
            {
                oddCount++;   // فردي
            }
        }
        
        // عرض النتائج
        // Display results
        Console.WriteLine("\n=== النتائج / Results ===");
        Console.WriteLine($"عدد الأرقام الزوجية (Even numbers): {evenCount}");
        Console.WriteLine($"عدد الأرقام الفردية (Odd numbers): {oddCount}");
        
        // انتظار لإغلاق البرنامج
        Console.WriteLine("\nاضغط أي مفتاح للخروج...");
        Console.ReadKey();
    }
}
```

### شرح الكود خطوة بخطوة / Step-by-Step Explanation:

1. **إنشاء المصفوفة**: `int[] numbers = new int[10];`
   - تخزن 10 أرقام صحيحة

2. **متغيرات العد**: `evenCount` و `oddCount`
   - لحساب عدد الأرقام الزوجية والفردية

3. **حلقة for للإدخال**: 
   - تطلب من المستخدم إدخال 10 أرقام

4. **حلقة foreach للعد**:
   - تمر على كل رقم في المصفوفة
   - تستخدم `%` (باقي القسمة) للتحقق من الزوجي/الفردي
   - إذا كان `num % 2 == 0` فالرقم زوجي
   - وإلا فهو فردي

5. **عرض النتائج**: طباعة عدد الزوجي والفردي

**مثال على التشغيل / Example Run:**
```
أدخل الرقم 1: 5
أدخل الرقم 2: 8
أدخل الرقم 3: 3
...
عدد الأرقام الزوجية: 4
عدد الأرقام الفردية: 6
```

---

## Q3: Answer only four from the following (أجب على أربعة فقط من التالي)

### 1. The correct definition of array is: (التعريف الصحيح للمصفوفة)

**الإجابة الصحيحة / Correct Answer: d. float [] x = new float [3];**

**الشرح / Explanation:**
- `float []` يعني مصفوفة من نوع float
- `x` هو اسم المصفوفة
- `new float [3]` ينشئ مصفوفة بـ 3 عناصر
- هذا هو الشكل الصحيح: `نوع_البيانات[] اسم_المصفوفة = new نوع_البيانات[الحجم];`

**الخيارات الخاطئة / Wrong Options:**
- a. `float [3] x;` - خطأ في موضع الأقواس
- b. `float [3] x = new float [];` - خطأ في الصيغة
- c. `float [3] x = new float [3];` - خطأ في موضع الأقواس

---

### 2. The correct definition of variables in a method: (التعريف الصحيح للمتغيرات في الدالة)

**الإجابة الصحيحة / Correct Answer: c. public static void Sum(params int[] k1, params int[] k2) { }**

**الشرح / Explanation:**
- `params` تسمح بإرسال عدد متغير من المعاملات
- يمكن استخدام `params` مع مصفوفات
- الصيغة الصحيحة: `params نوع[] اسم_المعامل`

**ملاحظة مهمة / Important Note:**
في الحقيقة، C# لا يسمح باستخدام أكثر من `params` واحد في نفس الدالة. الإجابة الأدق هي:
- إذا كان السؤال عن صيغة `params` بشكل عام، فالخيار c يوضح الصيغة الصحيحة
- لكن عملياً، يجب أن يكون `params` الأخير في قائمة المعاملات

---

### 3. The output on screen of the following codes: (ناتج الكود التالي)

```csharp
for (int i = 0; i < 10; i++)
{
    if (i == 2 || i == 6) { continue; }
    if (i == 4 && i == 8) { break; }
    Console.Write(i);
}
```

**الإجابة الصحيحة / Correct Answer: c. 01345789**

**الشرح خطوة بخطوة / Step-by-Step Explanation:**

| i | الشرط الأول<br/>First Condition | الشرط الثاني<br/>Second Condition | الإجراء<br/>Action | الناتج<br/>Output |
|---|---|---|---|---|
| 0 | لا (i ≠ 2 و i ≠ 6) | لا (0 == 4 كاذب) | طباعة | 0 |
| 1 | لا | لا | طباعة | 1 |
| 2 | **نعم** (i == 2) | - | **continue** (تخطي) | - |
| 3 | لا | لا | طباعة | 3 |
| 4 | لا | **لا** (4 == 4 صح لكن 4 == 8 خطأ) | طباعة | 4 |
| 5 | لا | لا | طباعة | 5 |
| 6 | **نعم** (i == 6) | - | **continue** (تخطي) | - |
| 7 | لا | لا | طباعة | 7 |
| 8 | لا | لا (8 == 4 كاذب) | طباعة | 8 |
| 9 | لا | لا | طباعة | 9 |

**النقاط المهمة / Key Points:**
- `continue`: يتخطى باقي الكود ويذهب للتكرار التالي
- `break`: يوقف الحلقة تماماً
- `&&` يعني AND (كلاهما يجب أن يكون صحيح)
- الشرط `i == 4 && i == 8` مستحيل (i لا يمكن أن يكون 4 و 8 في نفس الوقت!)
- لذلك `break` لن يحدث أبداً

**الناتج النهائي: 01345789**

---

### 4. The correct definition of construct method for class named A: (التعريف الصحيح لدالة البناء لكلاس A)

**الإجابة الصحيحة / Correct Answer: a. A() { }**

**الشرح / Explanation:**

دالة البناء (Constructor) لها قواعد خاصة:
1. **نفس اسم الكلاس**: يجب أن يكون اسمها مطابق لاسم الكلاس تماماً
2. **لا نوع إرجاع**: لا يوجد void أو int أو أي نوع آخر
3. **يمكن أن تكون public أو private**: لكن عادة تكون public

```csharp
public class A
{
    // دالة البناء الصحيحة
    public A()
    {
        Console.WriteLine("تم إنشاء كائن من كلاس A");
    }
}
```

**الخيارات الخاطئة / Wrong Options:**
- b. `a() { }` - حرف صغير (يجب أن يطابق اسم الكلاس بالضبط)
- c. `int A { }` - دالة البناء لا يكون لها نوع إرجاع
- d. `void A { }` - دالة البناء لا يكون لها نوع إرجاع

---

### 5. Th modifier that make code accessible within the same class, or in a class that is inherited: (المعدل الذي يجعل الكود قابل للوصول داخل نفس الكلاس أو في كلاس وارث)

**الإجابة الصحيحة / Correct Answer: c. protected**

**الشرح / Explanation:**

هناك 4 مستويات وصول رئيسية في C#:

| المعدل | الوصول من نفس الكلاس | الوصول من كلاس وارث | الوصول من خارج الكلاس |
|--------|---------------------|-------------------|---------------------|
| **public** | ✓ نعم | ✓ نعم | ✓ نعم |
| **private** | ✓ نعم | ✗ لا | ✗ لا |
| **protected** | ✓ نعم | ✓ نعم | ✗ لا |
| **internal** | ✓ نعم | ✓ نعم (في نفس المشروع) | ✓ نعم (في نفس المشروع) |

**مثال عملي / Practical Example:**

```csharp
public class Parent
{
    public int publicVar = 1;      // يمكن الوصول من أي مكان
    private int privateVar = 2;    // فقط داخل Parent
    protected int protectedVar = 3; // داخل Parent والكلاسات الوارثة
    internal int internalVar = 4;  // داخل نفس المشروع
}

public class Child : Parent
{
    public void TestAccess()
    {
        Console.WriteLine(publicVar);     // ✓ يعمل
        // Console.WriteLine(privateVar); // ✗ خطأ! لا يمكن الوصول
        Console.WriteLine(protectedVar);  // ✓ يعمل (لأن Child يرث من Parent)
        Console.WriteLine(internalVar);   // ✓ يعمل
    }
}
```

**لماذا protected هي الإجابة الصحيحة؟**
- السؤال يطلب: "داخل نفس الكلاس **أو** في كلاس وارث"
- `protected` تسمح بالوصول في كلا الحالتين
- `private` فقط داخل نفس الكلاس (ليست كافية)
- `public` تسمح للجميع (أكثر من المطلوب)

---

## ملخص الإجابات / Summary of Answers

| السؤال | الإجابة |
|--------|---------|
| Q1 | OOP is a programming paradigm based on objects (explained above) |
| Q2 | Complete C# program with array and foreach loop (code above) |
| Q3.1 | **d.** float [] x = new float [3]; |
| Q3.2 | **c.** public static void Sum(params int[] k1, params int[] k2) |
| Q3.3 | **c.** 01345789 |
| Q3.4 | **a.** A() { } |
| Q3.5 | **c.** protected |

---

## نصائح للمبتدئين / Tips for Beginners

1. **تدرب على الكود**: اكتب الكود بنفسك ولا تكتفي بالقراءة
   - Practice coding: Write code yourself, don't just read

2. **استخدم Visual Studio**: بيئة تطوير ممتازة للمبتدئين
   - Use Visual Studio: Excellent IDE for beginners

3. **جرب الأمثلة**: غير القيم وشوف النتائج
   - Try examples: Change values and see results

4. **اقرأ رسائل الأخطاء**: تساعدك على فهم المشاكل
   - Read error messages: They help you understand problems

5. **ابدأ بالأساسيات**: المتغيرات، الحلقات، الشروط، ثم انتقل للمواضيع المتقدمة
   - Start with basics: Variables, loops, conditions, then move to advanced topics

---

**تم إعداد هذا الملف بواسطة / Prepared by:** GitHub Copilot
**التاريخ / Date:** December 6, 2025

**حظاً موفقاً في دراستك! / Good luck with your studies!** 🎓
